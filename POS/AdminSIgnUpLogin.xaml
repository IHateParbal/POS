<Page x:Class="POS.AdminSIgnUpLogin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POS"
      mc:Ignorable="d" 
      d:DesignHeight="1010" d:DesignWidth="1920"
      Title="AdminSIgnUpLogin">

    <Page.Resources>
        <Style TargetType="TextBox">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Style.Resources>
        </Style>
        <Style TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Style.Resources>    
        </Style>

        <Style x:Key="RoundedGridStyle" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid>
                            <Border CornerRadius="10" Background="Gray">
                                <ContentPresenter Content="{TemplateBinding Content}" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="C:\Users\madgw\Downloads\gradient-white-monochrome-background\5617624.jpg"/>
        </Grid.Background>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label x:Name="ADMIN" Content="Admin Login" FontSize="95" Foreground="Gray"  FontWeight="UltraBold" HorizontalAlignment="Center" FontFamily="Harlow Solid Italic" Margin="10"/>

            <Grid>
                <ContentControl Style="{StaticResource RoundedGridStyle}" >
                    <StackPanel Margin="55">
                        <TextBox x:Name="UserName" FontWeight="Bold" FontSize="20" Foreground="Gray" VerticalContentAlignment="Center" Height="60" BorderThickness="0" Margin="20" HorizontalAlignment="Left" Width="400"/>
                        <TextBox x:Name="Password" FontWeight="Bold" FontSize="20" Foreground="Gray" VerticalContentAlignment="Center" Height="60" BorderThickness="0" Margin="20" HorizontalAlignment="Left" Width="400"/>
                        <Button Width="300" Height="80" Background="Orange" Content="LogIn" FontSize="40" FontWeight="SemiBold" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Foreground="gray" Margin="10" BorderThickness="0"/>
                    </StackPanel>
                </ContentControl>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
